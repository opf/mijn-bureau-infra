global:
  domain: "kubernetes.local"
  resourcesPreset: "small"

cluster:
  ingress:
    type: nginx

application:
  grist:
    namespace: grist

  ollama:
    namespace: ollama

  keycloak:
    namespace: keycloak

  chat:
    namespace: chat

  livekit:
    namespace: livekit

  nextcloud:
    namespace: nextcloud

ai:
  selfhost:
    enabled: true
    gpu:
      enabled: false
      type:
      number:
      mig:
        enabled:
        devices:
    resources:
      limits:
        memory: "4Gi"
        cpu: "750m"
  llm:
    model:
    endpoint:
    apiKey:
